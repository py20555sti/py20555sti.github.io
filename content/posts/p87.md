---
date: 2025-10-23T10:00:00-04:00
description: ""
featured_image: "images/featured_images/p87.jpg"
tags: ["impreza", "gc8", "type-r"]
title: "3*EJ20"
---

There was already a post about the [3*555]({{< relref "posts/p87" >}}), now
it's time for another trio: the EJ20. It's confirmed: the bearing waved
goodbye.

Interestingly, it turned out that the Type R doesn't have its original engine,
one it left the factory with, but a different one, from an earlier model
(93–96?). Crankshafts for those engines are no longer available, so even if I
had any doubts about whether to rebuild or drop in a new short block, the lack
of a crankshaft would have cleared them right up.

Looking a bit into the future, I can say this: everything ended well. The car
is back home, alive and well.

As a souvenir, I kept some bits from the old engine:

{{<gallery
    globalMatch="images/p87/old/p87_box_of_frogs.jpg"
    sortOrder="asc"
    rowHeight="150"
    margins="5"
    thumbnailResizeOptions="800x800 q90 Lanczos"
    showExif=true
    previewType="blur"
    embedPreview=true
    loadJQuery=true
>}}

Here's where things get a little ugly.

I'm not entirely sure this silicone is supposed to be dripping like that on the
inside:

{{<gallery
    globalMatch="images/p87/old/p87_oil_pickup.jpg"
    sortOrder="asc"
    rowHeight="150"
    margins="5"
    thumbnailResizeOptions="800x800 q90 Lanczos"
    showExif=true
    previewType="blur"
    embedPreview=true
    loadJQuery=true
>}}

What I am sure of is that the gold glitter in the oil pan is not a good sign:

{{<gallery
    globalMatch="images/p87/old/p87_oil_pan.jpg"
    sortOrder="asc"
    rowHeight="150"
    margins="5"
    thumbnailResizeOptions="800x800 q90 Lanczos"
    showExif=true
    previewType="blur"
    embedPreview=true
    loadJQuery=true
>}}

Bearings:

{{<gallery
    globalMatch="images/p87/old/p87_bearing_1.jpg"
    sortOrder="asc"
    rowHeight="150"
    margins="5"
    thumbnailResizeOptions="800x800 q90 Lanczos"
    showExif=true
    previewType="blur"
    embedPreview=true
    loadJQuery=true
>}}

The infamous fourth piston and connecting rod:

{{<gallery
    globalMatch="images/p87/old/piston/*"
    sortOrder="asc"
    rowHeight="150"
    margins="5"
    thumbnailResizeOptions="800x800 q90 Lanczos"
    showExif=true
    previewType="blur"
    embedPreview=true
    loadJQuery=true
>}}


And their friends:

{{<gallery
    globalMatch="images/p87/old/p87_piston_1_2_3.jpg"
    sortOrder="asc"
    rowHeight="150"
    margins="5"
    thumbnailResizeOptions="800x800 q90 Lanczos"
    showExif=true
    previewType="blur"
    embedPreview=true
    loadJQuery=true
>}}

Completely delaminated crankshaft:

{{<gallery
    globalMatch="images/p87/old/crank/*"
    sortOrder="asc"
    rowHeight="150"
    margins="5"
    thumbnailResizeOptions="800x800 q90 Lanczos"
    showExif=true
    previewType="blur"
    embedPreview=true
    loadJQuery=true
>}}


Block:

{{<gallery
    globalMatch="images/p87/old/block/*"
    sortOrder="asc"
    rowHeight="150"
    margins="5"
    thumbnailResizeOptions="800x800 q90 Lanczos"
    showExif=true
    previewType="blur"
    embedPreview=true
    loadJQuery=true
>}}

Looks like someone tried to "machine" it with sandpaper.

Inside of the cylinder:

{{<gallery
    globalMatch="images/p87/old/p87_cylinder.jpg"
    sortOrder="asc"
    rowHeight="150"
    margins="5"
    thumbnailResizeOptions="800x800 q90 Lanczos"
    showExif=true
    previewType="blur"
    embedPreview=true
    loadJQuery=true
>}}

Interestingly, before the sale it was filled with fresh oil:

{{<gallery
    globalMatch="images/p87/old/p87_auction.jpg"
    sortOrder="asc"
    rowHeight="150"
    margins="5"
    thumbnailResizeOptions="800x800 q90 Lanczos"
    showExif=true
    previewType="blur"
    embedPreview=true
    loadJQuery=true
>}}

And somehow, during the inspection, it seemed to run just fine, even when revved.
After it arrived in Poland, I drove it twice, about 2–3 km each time with no
problem. It wasn't until I took it for a longer drive, when the engine got
fully warmed up, the turbo spooled, and it got a bit of a kick up and down the
gears, that everything finally let go.

The rest you can see in the photos above.

But that's all history now.

As for the present, my local Subaru specialist:
[Carfit](https://www.carsport.pl) rose to the occasion and built a new heart,
right in time for the car to start the conversion in two days time.

A few photos from the rebuild.

{{<gallery
    globalMatch="images/p87/shortblock/*"
    sortOrder="asc"
    rowHeight="150"
    margins="5"
    thumbnailResizeOptions="800x800 q90 Lanczos"
    showExif=true
    previewType="blur"
    embedPreview=true
    loadJQuery=true
>}}

And if anyone is still wondering whether the author of this blog has a screw
loose, here is your answer:

{{<gallery
    globalMatch="images/p87/p87_label.jpg"
    sortOrder="asc"
    rowHeight="150"
    margins="5"
    thumbnailResizeOptions="800x800 q90 Lanczos"
    showExif=true
    previewType="blur"
    embedPreview=true
    loadJQuery=true
>}}

The top one is the most important, but the rest are going into the binder too.

{{<gallery
    globalMatch="images/p87/p87_labels.jpg"
    sortOrder="asc"
    rowHeight="150"
    margins="5"
    thumbnailResizeOptions="800x800 q90 Lanczos"
    showExif=true
    previewType="blur"
    embedPreview=true
    loadJQuery=true
>}}
